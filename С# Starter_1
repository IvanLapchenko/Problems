class Program
{
    public static void Main(string[] args)
    {
        SquareSum();
    }

    public static void SquareSum()
    {
        string input_data;
        int out_data = 0;

        while (true)
        {
            input_data = Console.ReadLine();

            if (input_data == "")
                break;
            else
                a = Convert.ToInt32(input_data);
                out_data += //math.pow(a);
        };
        System.Console.WriteLine(out_data);
    }

}
